export interface ActionCardContentInterface {
    id: number;
    // title: string;
    // addText: string;
    rowTitle: string;
    badge?: string;
    isDeletableAction?: boolean;
    rowList: string[];
}
